package model;

import org.apache.logging.log4j.Logger;
import org.openqa.selenium.WebDriver;

public class NewObjectPage extends AbstractPage {
    NewObjectPage(WebDriver driver, Logger log) {
        super(driver, log);
    }
}
